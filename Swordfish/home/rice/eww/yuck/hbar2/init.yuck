(include "./yuck/hbar2/layout.yuck")

;; 14 is the amount of pixels between windows
(defwindow hbar2
  :monitor 1
  :geometry (geometry
              :x "0%"
              :y "14px"
              :width "200px"
              :height "15px"
              :anchor "top center")

  :stacking "bg"
  :exclusive true
  (hbar_layout2))
